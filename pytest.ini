[pytest]
testpaths = tests
# So `pytest` finds `app` however it is invoked — bare, as `python -m pytest`,
# or from a CI runner whose working directory is not the repository root.
pythonpath = .
addopts = -q
